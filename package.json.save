git add package.json
git rebase --continue 2>/dev/null || git merge --continue 2>/dev/null || true
git push origin main
git add package.json
git rebase --continue 2>/dev/null || git merge --continue 2>/dev/null || true
git add package.json
git rebase --continue 2>/dev/null || git merge --continue 2>/dev/null || true
git push origin main
git add package.json
git rebase --continue 2>/dev/null || git merge --continue 2>/dev/null || true
git push origin main

{
  "name": "musicmaykers-platform",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.0"
  }
}
